import Plot from "react-plotly.js";

export default function GrapIntegrate({myGraph}){
    console.log("data in Graph:", myGraph);

    if(!myGraph || myGraph.lenght === 0) return null;

    <div style={{width:"100"}}>
        <Plot 
        data={[
            
        ]}
        
        
        
        
        />

    </div>

}